% This script adds all necessary paths for the execution of the script

% run this script from within the 'main' folder

disp('Adding paths to the workspace...')

addpath(genpath('../database'));
addpath(genpath('../mySynGrasp'));
addpath(genpath('../Toolbox'));
addpath(genpath('../src'));
addpath(genpath('pwd'));